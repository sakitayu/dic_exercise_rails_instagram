<h1>Pachistagramのほ〜む画面(タイムライン)</h1>

<%= link_to '投稿する', new_blog_path %>


<% @blogs.each do |blog| %>
<br>
<br>
<table>
  <tr>
    <td>投稿者→<%= link_to "#{blog.user.name}", user_path(blog.user.id) %></td>
    <td>投稿日→<%= blog.user.created_at.try(:strftime,"%m月%d日") %></td>
    <td><%= link_to '詳細', blog_path(blog.id) %></td>
    <% if current_user.id == blog.user.id %>
    <td><%= link_to "編集", edit_blog_path(blog.id) %></td>
    <td>
      <%= link_to '削除', blog_path(blog.id), method: :delete ,data: { confirm: '本当に削除していいですか？' } %>
    </td>
    <% end %>
  </tr>
  <tr>
    <td>
    <% if blog.image.file.nil? %>
      <% else %>
      <%= image_tag blog.image.url %>
    <% end %>
    </td>
    <td>
    <% @favorite = current_user.favorites.find_by(blog_id: blog.id) %>
    <% if @favorite.present? %>
    <%= link_to 'お気に入り解除する', favorite_path(id: @favorite.id), method: :delete, class: 'btn btn-danger' %>
    <% else %>
    <%= link_to 'お気に入りする', favorites_path(blog_id: blog.id), method: :post, class: 'btn btn-primary' %>
    <% end %>
    </td>
  </tr>
  <tr>
    <td>文章→<%= blog.content %></td>
  </tr>
</table>
<% end %>
</table>

<br>

